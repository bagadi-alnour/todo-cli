#!/bin/sh
# Remove Cursor agent co-author trailer before the commit is finalized.
path="${1:?}"
[ -f "$path" ] || exit 0
tmp="${path}.strip.$$"
sed '/^Co-authored-by: Cursor <cursoragent@cursor.com>$/d' "$path" >"$tmp" && mv "$tmp" "$path"
exit 0
